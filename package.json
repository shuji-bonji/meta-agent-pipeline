{
  "name": "meta-agent-pipeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Solo developer's autonomous AI dev pipeline: Issue→Deploy via Meta-agent + Sub-agent (cloud + local)",
  "repository": {
    "type": "git",
    "url": "https://github.com/shuji-bonji/meta-agent-pipeline"
  },
  "author": "shuji-bonji",
  "license": "MIT",
  "workspaces": [
    "shared",
    "cloud",
    "local"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "typecheck": "tsc -b --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
